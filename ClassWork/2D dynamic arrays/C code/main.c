#include <stdio.h>
#include <stdlib.h>

int main(void)
{
    int iRows;
    int iCols;
    int iCounter1;
    int iCounter2;
    int **pPtr;
    printf("Enter number of rows\t");
    scanf("%d", &iRows);

    printf("\nEnter number of cols\t");
    scanf("%d", &iCols);

    pPtr = (int**)malloc(iRows*sizeof(int*));

    





    exit(0);
}